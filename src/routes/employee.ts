import { Router } from "express";

const router = Router();


router.get("/",);
router.post("/", (req, res) => {});
router.put("/", (req, res) => {});
router.delete("/", (req, res) => {});

export default router;

